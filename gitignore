# المجلدات الناتجة عن تثبيت المكتبات (أهم سطر)
node_modules/

# المجلدات الناتجة عن بناء المشروع النهائي للرفع (Build outputs)
dist/
dist-ssr/
*.local

# ملفات الحزم المؤقتة الخاصة بـ الأداء والتشغيل
.cache/
.parcel-cache/

# ملفات المتغيرات البيئية السرية (مثل كلمات مرور قواعد البيانات أو الـ APIs)
.env
.env.local
.env.*.local

# ملفات سجلات الأخطاء (Logs)
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ملفات النظام المؤقتة (خاصة بماك وويندوز)
.DS_Store
Thumbs.db

# ملفات إعدادات محررات الأكواد الشخصية (اختياري)
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp
